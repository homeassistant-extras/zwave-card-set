import { css } from 'lit';

export const styles = css`
  span {
    font-size: 1.2em;
    font-weight: bold;
  }

  .devices {
    margin-top: 10px;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
`;
